import { Country } from '../interfaces/country';
export const SIGNED_IN_USER: string = 'voicemod_signed_in_user';
export const MAX_USER_RECORDS: number = 10;
export const DEFAULT_COUNTRY: Country = { id: '-1', name: 'Please select a country' };
